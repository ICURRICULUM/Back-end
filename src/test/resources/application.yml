spring:
  application:
    name: icurriculum-test

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 5.0.5

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
